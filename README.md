# EasyStateMachine
EasyStateMachine is a Godot 4 add-on that provides a modular, self-contained finite state machine (FSM) system adaptable to both 2D and 3D projects. Unlike homemade solutions, it’s built for scalability: each state exists as its own node, is managed through a custom inspector, and offers a clean API to handle a wide range of use cases.
